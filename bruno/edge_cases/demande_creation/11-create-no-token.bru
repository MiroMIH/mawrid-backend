meta {
  name: 11-create-no-token
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/buyer/demandes
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "title": "Valid Title",
    "quantity": 100,
    "deadline": "2026-12-31",
    "categoryId": 12,
    "wilaya": "16"
  }
}

tests {
  test("status 401", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
