meta {
  name: 01-close-owned
  type: http
  seq: 1
}

patch {
  url: {{baseUrl}}/buyer/demandes/{{demandeId}}/close
  body: none
  auth: bearer
}

auth:bearer {
  token: {{buyerToken}}
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
