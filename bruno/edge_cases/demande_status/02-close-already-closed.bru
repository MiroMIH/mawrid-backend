meta {
  name: 02-close-already-closed
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/buyer/demandes/{{demandeId}}/close
  body: none
  auth: bearer
}

auth:bearer {
  token: {{buyerToken}}
}

tests {
  test("status 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
