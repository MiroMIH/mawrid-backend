meta {
  name: Access Admin as Buyer
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/admin/stats
  body: none
  auth: bearer
}

auth:bearer {
  token: {{buyerToken}}
}

tests {
  test("status 401 — Spring Security returns 401 for wrong role", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
