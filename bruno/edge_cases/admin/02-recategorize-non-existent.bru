meta {
  name: Recategorize Non Existent
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/admin/demandes/{{demandeId}}/recategorize
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "newCategoryId": 9999
  }
}

tests {
  test("status 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
