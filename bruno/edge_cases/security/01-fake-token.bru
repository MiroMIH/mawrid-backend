meta {
  name: Fake Token
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/users/me
  body: none
  auth: bearer
}

auth:bearer {
  token: fake.token.here
}

tests {
  test("status 401", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
