meta {
  name: Page 999
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/buyer/demandes?page=999
  body: none
  auth: bearer
}

auth:bearer {
  token: {{buyerToken}}
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
