meta {
  name: Move Category (Admin)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/categories/{{categoryId}}/move
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "newParentId": 2
  }
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
