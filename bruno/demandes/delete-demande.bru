meta {
  name: Delete Demande (Buyer/Admin)
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/demandes/{{demandeId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 204 or 200", function() {
    expect([200, 204]).to.include(res.getStatus());
  });
}
