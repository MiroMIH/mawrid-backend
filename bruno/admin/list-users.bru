meta {
  name: List Users (Admin)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/admin/users?page=0&size=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  note: Login as ADMIN before calling any admin endpoint.
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
