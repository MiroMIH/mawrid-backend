meta {
  name: List Reponses for Demande (Buyer/Admin)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/demandes/{{demandeId}}/reponses?page=0&size=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
